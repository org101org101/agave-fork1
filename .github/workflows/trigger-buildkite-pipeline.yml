name: Trigger Buildkite Pipeline (Pull Request)

on:
  pull_request_target:
    branches:
      - master
      - v[0-9]+.[0-9]+
    types:
      - opened
      - synchronize
      - reopened
      - labeled

jobs:
  check:
    runs-on: ubuntu-24.04
    if: >-
      (github.event.action == 'labeled' && github.event.label.name == 'CI') ||
      (github.event.action != 'labeled' && contains(fromJSON('["OWNER","MEMBER","COLLABORATOR"]'), github.event.pull_request.author_association))
    steps:
      - name: Check member or CI label
        id: check
        env:
          ACTION: ${{ github.event.action }}
          LABEL_NAME: ${{ github.event.label.name }}
        run: |
          if [ "$ACTION" = "labeled" ]; then
            TRIGGER_TYPE="CI Label"
          else
            TRIGGER_TYPE="Pull Request Event"
          fi

          echo "label_name=$LABEL_NAME" | tee -a $GITHUB_OUTPUT
          echo "should_trigger=true" | tee -a $GITHUB_OUTPUT
          echo "trigger_type=$TRIGGER_TYPE" | tee -a $GITHUB_OUTPUT
   
  trigger:
    runs-on: ubuntu-24.04
    needs:
      - check
    steps:
      - name: Remove CI label
        if: github.event.action == 'labeled'
        uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3 # v9.0.0
        with:
          script: |
            try {
              await github.rest.issues.removeLabel({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.payload.pull_request.number,
                name: 'CI',
              });
            } catch (error) {
              if (error.status === 404) {
                core.warning('CI label was already removed.');
              } else {
                throw error;
              }
            }

      - name: Prepare Buildkite pipeline info
        id: prepare
        env:
          BUILDKITE_PIPELINE: ${{ vars.BUILDKITE_PIPELINE }}
          COMMIT: ${{ github.event.pull_request.head.sha }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          TITLE: ${{ github.event.pull_request.title }}
        run: |
          COMMIT_SHORT="${COMMIT:0:8}"
          BRANCH="pull/${PR_NUMBER}/head"
          MESSAGE="PR#${PR_NUMBER} - ${COMMIT_SHORT} - ${TITLE}"
          BUILD_META_DATA="{\"pr_number\":\"${PR_NUMBER}\"}"

          echo "branch=$BRANCH" | tee -a $GITHUB_OUTPUT
          echo "build_meta_data=$BUILD_META_DATA" | tee -a $GITHUB_OUTPUT
          echo "commit=$COMMIT" | tee -a $GITHUB_OUTPUT
          echo "message=$MESSAGE" | tee -a $GITHUB_OUTPUT
          echo "pipeline=$BUILDKITE_PIPELINE" | tee -a $GITHUB_OUTPUT
          echo "pr_number=$PR_NUMBER" | tee -a $GITHUB_OUTPUT

          {
            echo "## Buildkite Pipeline Info"
            echo ""
            echo "| Key | Value |"
            echo "|-----|-------|"
            echo "| Pipeline | \`$BUILDKITE_PIPELINE\` |"
            echo "| Branch | \`$BRANCH\` |"
            echo "| Commit | \`$COMMIT\` |"
            echo "| PR Number | #$PR_NUMBER |"
            echo "| Message | $MESSAGE |"
          } >> $GITHUB_STEP_SUMMARY

      - name: Trigger a Buildkite Build
        if: steps.check.outputs.should_trigger == 'true'
        uses: "buildkite/trigger-pipeline-action@909fed762c73d5ae2b5d555ab910d66b3fae2670" # v2.4.1
        with:
          pipeline: ${{ steps.prepare.outputs.pipeline }}
          buildkite_api_access_token: ${{ secrets.BUILDKITE_API_ACCESS_TOKEN }}
          branch: "${{ steps.prepare.outputs.branch }}"
          build_meta_data: "${{ steps.prepare.outputs.build_meta_data }}"
          commit: "${{ steps.prepare.outputs.commit }}"
          message: "${{ steps.prepare.outputs.message }}"
